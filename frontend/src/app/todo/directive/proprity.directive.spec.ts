import { ProprityDirective } from './proprity.directive';

describe('ProprityDirective', () => {
  it('should create an instance', () => {
    const directive = new ProprityDirective();
    expect(directive).toBeTruthy();
  });
});
